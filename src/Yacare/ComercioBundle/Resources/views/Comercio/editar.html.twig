{% extends 'TapirAbmBundle::editar.html.twig' %}

{% block pagina_titulo %}{% if entity.id %}{{ entity.Nombre }}{% else %}Nuevo comercio{% endif %}{% endblock %}

{% block pagina_contenido %}
<div class="row form-group">
	<label class="col-sm-2 control-label">Nombre de fantasía</label>
    <div class="col-sm-10">{{ form_widget(edit_form.Nombre) }}</div>
</div>

<div class="row form-group">
	<label class="col-sm-2 control-label">Titular</label>
    <div class="col-sm-10">{{ form_widget(edit_form.Titular) }}</div>
</div>

<div class="row form-group">
	<label class="col-sm-2 control-label">Apoderado</label>
    <div class="col-sm-10">{{ form_widget(edit_form.Apoderado) }}</div>
</div>

<div class="row form-group">
	<label class="col-sm-2 control-label">Local comercial</label>
    <div class="col-sm-10">{{ form_widget(edit_form.Local) }}</div>
</div>

<div class="row form-group">
	<label class="col-sm-2 control-label">Expendiente</label>
    <div class="col-sm-4">{{ form_widget(edit_form.ExpedienteNumero) }}</div>

	<label class="col-sm-1 control-label">Estado</label>
    <div class="col-sm-5">{{ form_widget(edit_form.Estado) }}</div>
</div>

<div class="row">
	<div class="col-xs-12">
		<h3>Actividades comerciales</h3>
		<p>Seleccione hasta 6 actividades comerciales.</p>
	</div>
</div>
<div class="row form-group">
	<div class="col-xs-12 col-sm-6">{{ form_widget(edit_form.Actividad1) }}</div>
	<div class="col-xs-12 col-sm-6">{{ form_widget(edit_form.Actividad2) }}</div>
</div>
<div class="row form-group">
	<div class="col-xs-12 col-sm-6">{{ form_widget(edit_form.Actividad3) }}</div>
	<div class="col-xs-12 col-sm-6">{{ form_widget(edit_form.Actividad4) }}</div>
</div>
<div class="row form-group">
	<div class="col-xs-12 col-sm-6">{{ form_widget(edit_form.Actividad5) }}</div>
	<div class="col-xs-12 col-sm-6">{{ form_widget(edit_form.Actividad6) }}</div>
</div>

{{ form_rest(edit_form) }}
{% endblock pagina_contenido %}
